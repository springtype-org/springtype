{
  "name": "@springtype/springtype",
  "version": "1.0.0-alpha.0",
  "private": true,
  "description": "SpringType meta package.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/springtype-org/springtype.git"
  },
  "scripts": {
    "test": "lerna run test",
    "prune": "lerna run prune",
    "clean": "lerna run clean",
    "publish": "npm login && lerna build && lerna publish",
    "build": "lerna run build",
    "bootstrap": "lerna exec npm install",
    "packages": "lerna ls --sort",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  },
  "author": "Aron Homberg, Michael Mannseicher",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/springtype-org/springtype/issues"
  },
  "homepage": "https://springtype.org",
  "devDependencies": {
    "lerna": "^3.14.1",
    "typescript": "^3.3.4000"
  },
  "dependencies": {
    "node-gyp": "^3.8.0",
    "npm": "^6.9.0",
    "semver": "^6.1.1",
    "springtype": "^1.0.0-alpha.1.33",
    "st-create": "^1.0.0-alpha.1.33"
  }
}
