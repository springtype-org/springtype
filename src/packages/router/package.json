{
    "name": "@springtype/router",
    "version": "1.0.0-alpha.1.38",
    "publishConfig": {
        "access": "public"
    },
    "description": "DOM router for SpringType.",
    "dependencies": {
        "@springtype/core": "file:../core/dist",
        "@types/node": "^11.10.5",
        "reflect-metadata": "^0.1.12",
        "tslib": "^1.9.3"
    },
    "local:main": "src/index.ts",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/springtype-org/springtype.git"
    },
    "scripts": {
        "bootstrap": "npm run bootstrap:router --prefix ../../",
        "clean:all": "npm run clean:router --prefix ../../",
        "clean": "npm run clean:router --prefix ../../",
        "build": "npm run build:router --prefix ../../",
        "watch": "npm run watch:router --prefix ../../",
        "release": "npm run release:router --prefix ../../",
        "test": "npm run test:router --prefix ../../"
    },
    "author": "Aron Homberg",
    "license": "MIT",
    "browserslist": [
        "last 1 Chrome version"
    ],
    "devDependencies": {
        "typescript": "^3.3.4000"
    }
}
