{
    "name": "@springtype/ssr",
    "version": "1.0.0-alpha.1.38",
    "publishConfig": {
        "access": "public"
    },
    "description": "JSDOM based server-side rendering for SpringType.",
    "dependencies": {
        "@springtype/core": "../core",
        "@types/node": "^11.10.5",
        "reflect-metadata": "^0.1.12",
        "tslib": "^1.9.3"
    },
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/springtype-org/springtype.git"
    },
    "scripts": {
        "bootstrap": "npm run bootstrap:ssr --prefix ../../",
        "clean:all": "npm run clean:ssr --prefix ../../",
        "clean": "npm run clean:ssr --prefix ../../",
        "build": "npm run build:ssr --prefix ../../",
        "watch": "npm run watch:ssr --prefix ../../",
        "release": "npm run release:ssr --prefix ../../",
        "test": "npm run test:ssr --prefix ../../"
    },
    "author": "Aron Homberg",
    "license": "MIT",
    "browserslist": [
        "last 1 Chrome version"
    ],
    "devDependencies": {
        "@tbranyen/jsdom": "^13.0.0",
        "typescript": "^3.3.4000"
    }
}
